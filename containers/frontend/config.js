const config = {
    /* FOR WHEN BOTH ARE RUN OUT OF DOCKER CONTAINERS */
    // API_URL: '//0.0.0.0:8000/api/',

    /* NORMAL */
    API_URL: '/api/',
    PACKAGES_ENDPOINT: 'packages/'
};

export default config;
